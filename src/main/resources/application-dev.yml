server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:biblioteca_virtual}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:121105}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: false
#    baseline-on-migrate: true
#    locations: classpath:db/migration
  security:
    user:
      name: devuser
      password: devpass
      jwt:
        secret: ${JWT_SECRET:12345678}
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USER:pedro.romanowski02@gmail.com}
    password: ${MAIL_PASSWORD:kkybdorqohqsrusm}
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000


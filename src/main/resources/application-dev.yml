spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:biblioteca_virtual}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:121105}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  flyway:
    enabled: false
  security:
    user:
      name: devuser
      password: devpass
      jwt:
        secret: ${JWT_SECRET}
